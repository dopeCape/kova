{
  "name": "core",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "go build -o bin/core ./cmd",
    "dev": "make dev",
    "test": "go test ./...",
    "lint": "golangci-lint run",
    "clean": "rm -rf bin",
    "tidy": "go mod tidy"
  }
}
